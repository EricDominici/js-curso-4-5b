const saludar = (nombre) => {
    console.log('Creando etiqueta h1. en 21 HTML! ');
    
    // Contentarm de una sola Unea.
    
    // El codigo camentado no lo lee el lnterpmte de JS.
    /*
    comentario de varias lineas.
    uso basico del DOCUMENTO OBJECT MODEL (DOM)
    permite a JS modificar el HTML.
    */
    
    
    const h1 = document.createElement('h1');
    h1.inneText = `Hola, ${nomre}`;
    
    document.body.append(hl);
    }
    const user = 'Juancho';
    saludar(user);
    // Eric Dominici #4